import React from 'react';
import PropTypes from 'prop-types';

const Body = (props) => {

  Body.propTypes = {

  };

    return (
        <div className="body-wrapper">
            <h1>Body</h1>
        </div>
    );
};

export default Body;
